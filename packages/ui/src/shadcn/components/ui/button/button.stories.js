import Button from './myButton.svelte'

export default { component: Button, tags: ['autodocs'], title: 'Shadcn/Button' }

export const Default = {
	args: {
		size: 'default',
		variant: 'default',
	},
}
