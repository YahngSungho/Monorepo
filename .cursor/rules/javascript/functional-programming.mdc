---
description:
globs: *.js,*.svelte,*.ts,*.mjs,*.cjs,*.mts,*.cts
alwaysApply: false
---
Use functional programming techniques whenever possible.
- Prefer to use Ramda. You can import Ramda by `import { R } from '@library/helpers/R'` in this codebase.
e.g. `myArray1.concat(myArray2)` -> `R.concat(myArray1, myArray2)`