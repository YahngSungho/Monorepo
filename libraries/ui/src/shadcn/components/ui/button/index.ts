import  {
	type ButtonProps,
} from "./button.svelte";

export {
	//
	
	type ButtonProps as Props,
};

export {default as Button, type ButtonSize, type ButtonVariant, buttonVariants, default as Root, type ButtonProps} from "./button.svelte";